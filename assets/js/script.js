// Collect and store user's search input and date
    // If a park name, search for exact or close matches
    // If state code, search for XX number of random sites in that state

// Fetch from NPS API using search parameters
    // Store the latitude/longitude of the found campsites for 
    
// 


// If the user clicks fav-btn: 
    // If checking the box, change the checkbox style and to "filled" and store camp-name, park-code, address, geo-coordinates in localStorage.
    // 
    
